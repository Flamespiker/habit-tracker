// TODO: Render the daily check-in page.
// Load today's habits and show a checklist to mark each as complete/incomplete.
// Allow adding a mood score and free-text journal note for the day.
// On submit, POST to /api/log and trigger AI coaching insight generation via /api/ai.

export default function LogPage() {
  return (
    <main>
      <h1>Daily Log</h1>
    </main>
  );
}
