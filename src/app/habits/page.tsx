// TODO: Fetch all habits for the current user from Supabase and display them in a list.
// Should support filtering by status (active/archived) and sorting by name or creation date.
// Each item links to /habits/[id]. Include a link to /habits/new to create a habit.

export default function HabitsPage() {
  return (
    <main>
      <h1>Habits</h1>
    </main>
  );
}
