// TODO: Render a form to create a new habit.
// Fields: name, description, frequency (daily/weekly/custom), target count, color/icon.
// On submit, POST to /api/habits and redirect to /habits/[id].

export default function NewHabitPage() {
  return (
    <main>
      <h1>New Habit</h1>
    </main>
  );
}
