// TODO: Render a form to create a new goal.
// Fields: title, description, target date, success criteria, linked habits (multi-select).
// On submit, POST to /api/goals and redirect to /goals/[id].

export default function NewGoalPage() {
  return (
    <main>
      <h1>New Goal</h1>
    </main>
  );
}
