// TODO: Fetch all goals for the current user from Supabase and display them in a list.
// Show goal title, target date, progress percentage, and linked habits.
// Support filtering by status (active/completed/abandoned). Link to /goals/new to create a goal.

export default function GoalsPage() {
  return (
    <main>
      <h1>Goals</h1>
    </main>
  );
}
