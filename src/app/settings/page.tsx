// TODO: Display user account and app settings.
// Sections: profile (display name, avatar), notification preferences, theme (light/dark),
// data export, and account deletion. Persist changes via PATCH /api/settings.
// User preferences (theme, notifications) stored in MongoDB; profile data in Supabase.

export default function SettingsPage() {
  return (
    <main>
      <h1>Settings</h1>
    </main>
  );
}
