// TODO: Render the sign-up form.
// Fields: email, password, confirm password. Submit calls Supabase signUp via /api/auth/signup.
// Show validation errors inline. On success, redirect to / (dashboard) or an email-verification notice.
// Include a link to /login for existing users.

export default function SignupPage() {
  return (
    <main>
      <h1>Sign Up</h1>
    </main>
  );
}
