// TODO: Render the login form.
// Fields: email, password. Submit calls Supabase signInWithPassword via /api/auth/login.
// Show validation errors inline. On success, redirect to / (dashboard).
// Include a link to /signup for new users and a "Forgot password?" flow.

export default function LoginPage() {
  return (
    <main>
      <h1>Login</h1>
    </main>
  );
}
